# Copie para .env e ajuste (não commitar segredos)

# Origem para migracao de dados (Etapa 4)
# SUPABASE_URL=https://seu-projeto.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=

# Postgres local (Docker, porta host 5435) — em dev a API e `npm run migrate` usam este valor
# (migrate carrega .env automaticamente; sem .env em dev usa o mesmo default local)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5435/agenciadev
# ADMIN_EMAIL=admin@agenciadev.com
# ADMIN_PASSWORD=trocar-por-senha-forte
# BCRYPT_ROUNDS=12
# FRONTEND_ORIGIN=http://localhost:9090

# API local (server/health.mjs) — padrão 3005
# PORT=3005
# JWT_SECRET=trocar-por-chave-longa
# JWT_EXPIRES_IN=7d
# UPLOAD_DIR=./uploads
# UPLOAD_PUBLIC_BASE_URL=http://localhost:3005
